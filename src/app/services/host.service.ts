import { Injectable } from '@angular/core';
//import { SwitchAppService } from '@nap/platform-client';

@Injectable()
export class HostService {
  //constructor(private switchAppService: SwitchAppService) {}

  onSwitchApp(uuid: string) {
    //this.switchAppService.change(uuid);
  }
}
